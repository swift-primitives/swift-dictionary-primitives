name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Build + test matrix — see swift-primitives/.github/.github/workflows/swift-ci.yml (layer wrapper, mandates embedded build)
  ci:
    uses: swift-primitives/.github/.github/workflows/swift-ci.yml@main
    with:
      cache-key-prefix: swift63-spm-v1
      enable-private-repos: true
    secrets:
      PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}

  # DocC umbrella-catalog pipeline per [DOC-019a] — see
  # swift-institute/.github/.github/workflows/swift-docs.yml
  docs:
    uses: swift-institute/.github/.github/workflows/swift-docs.yml@main
    with:
      umbrella-module: Dictionary_Primitives
      umbrella-display-name: "Dictionary Primitives"
      umbrella-bundle-id: "swift-dictionary-primitives.Dictionary-Primitives"
      umbrella-docc-path: "Sources/Dictionary Primitives/Dictionary Primitives.docc"
      exclude-modules: "Dictionary_Primitives_Test_Support"
      enable-private-repos: true
    secrets:
      PRIVATE_REPO_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
